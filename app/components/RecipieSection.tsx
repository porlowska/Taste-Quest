import RecipeCard from "./RecipeCard";


export default function RecipeSection() {

  return (
    <section>
        <div className="mt-3 flex flex-col md:flex-row wrap justyfy-evenly">
      {/* {data.forums.map((card, index) => (
            <RecipeCard 
              key={index} 
              icon={card.icon} 
              title={card.title} 
            />
        ))} */}
    </div>
    </section>
  );
}

